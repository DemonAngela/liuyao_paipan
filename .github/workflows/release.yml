name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: 配置 Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
          cache: pip
          cache-dependency-path: requirements-dev.lock

      - name: 配置 Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"

      - name: 安装锁定依赖
        run: python -m pip install --require-hashes -r requirements-dev.lock

      - name: 发布前验收
        shell: bash
        run: |
          python -m ruff check backend tests init_project.py
          python -m compileall -q backend init_project.py
          for file in frontend/js/*.js; do
            node --check "$file"
          done
          python -m pytest

      - name: 生成源码包与校验和
        shell: bash
        run: |
          mkdir -p dist
          archive_name="liuyao_paipan-${GITHUB_REF_NAME}.zip"
          archive="dist/${archive_name}"
          git archive \
            --format=zip \
            --prefix="liuyao_paipan-${GITHUB_REF_NAME}/" \
            --output="$archive" \
            HEAD
          (
            cd dist
            sha256sum "$archive_name" > "${archive_name}.sha256"
          )

      - name: 创建 GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          archive="dist/liuyao_paipan-${GITHUB_REF_NAME}.zip"
          gh release create "$GITHUB_REF_NAME" \
            "$archive" \
            "${archive}.sha256" \
            --verify-tag \
            --generate-notes \
            --title "$GITHUB_REF_NAME"
