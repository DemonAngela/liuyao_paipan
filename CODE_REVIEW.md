# 代码与功能审查报告

审查日期：2026-07-30  
项目：`E:\PythonProject\liuyao_paipan`

## 修复验收结论

截至 2026-07-30，原审查确认的 4 项 P1、8 项 P2 和 9 项 P3 已全部关闭；
修复分支为 `fix/comprehensive-review-remediation`。已在全新虚拟环境按哈希
锁安装依赖，并通过 48 项测试、Ruff、Python 编译、全部 JavaScript 语法、
数据生成幂等、Windows PowerShell 5.1 服务生命周期及真实 Chromium 四种
起卦流程验收。

| 编号 | 修复结果 | 关键证据 |
|---|---|---|
| P1-1 | 已修复月柱及历法边界 | `backend/core/ganzhi.py` 使用固定版 `lunar-python` 节气结果，权威日期样本通过 |
| P1-2 | 已实现梅花时间起卦 | `backend/core/meihua.py` 使用农历月日及年支、时支，原典样本通过 |
| P1-3 | 已建立严格输入边界 | `backend/models/gua.py` 校验类型、六爻长度、日期组合和范围，非法输入稳定返回 422 |
| P1-4 | 已补回归保障 | `tests/` 共 48 项，覆盖核心算法、API、前端契约和应用配置 |
| P2-5 | 已修正铜钱概率 | 四种爻象按三枚独立铜钱形成 `1/8、3/8、3/8、1/8` |
| P2-6 | 已修正省略时间行为 | 时间起卦省略时间时使用当前完整时刻 |
| P2-7 | 已修正前端失败链路 | 统一 `requestJson`、完整 `await` 和 `aria-live` 中文错误状态 |
| P2-8 | 已统一前后端流程 | 自动、时间、指定、手摇分别调用对应起卦 API，再统一调用排盘 API |
| P2-9 | 已重建纳甲实现 | `backend/core/najia.py` 以 `64gua_full.json` 为唯一运行时数据源 |
| P2-10 | 已清理必错公共方法 | 生克模块仅保留可测试公开入口，删除重复实现和 monkey patch |
| P2-11 | 已纠正准确性宣称 | README 与代码将相关内容明确为“项目展示规则” |
| P2-12 | 已收紧安全部署边界 | 默认回环监听、关闭 reload、同源 CORS、白名单禁用 `*`、隐藏内部异常 |
| P3-1 | 已清理空实现和重复请求模型 | 实现 `GuaciManager`，删除空校验器，请求模型改为单一来源导出 |
| P3-2 | 已清理重复字段和导入副作用 | 删除重复模型字段及模块级示例执行 |
| P3-3 | 已清理引擎静默降级 | 删除备用类、导入时补丁和隐藏依赖错误的回退 |
| P3-4 | 已消除重复事件监听 | 指定面板只绑定一次事件 |
| P3-5 | 已修正初始 UI 与可访问性 | 空结果默认隐藏，增加 favicon、标签、状态区和按钮语义 |
| P3-6 | 已消除前端 64 卦映射副本 | 卦辞、爻辞改由后端按卦名查询 |
| P3-7 | 已明确服务生命周期 | 一键入口以前台 CMD 承载服务并随窗口关闭；独立后台启停仍复用带 PID 身份校验、健康等待和日志的 PowerShell 脚本 |
| P3-8 | 已建立工程交付链 | Git、哈希锁、CI、标签发布及校验和已配置，备份脚本已删除 |
| P3-9 | 已消除编译警告 | `init_project.py` 的反斜杠转义已修正，编译检查通过 |

尚存的领域边界不是代码缺陷：六爻流派规则存在差异，生克展示规则尚未经过
外部专家统一校勘，因此文档不作超出已验证算法的准确性承诺。

## 原始审查结论（修复前）

当前版本可以完成页面起卦、排盘和卦辞展示，64 卦静态数据及 4,096 种本卦/动爻组合的结构验证均通过；但**月柱算法、时间起卦规则和 API 输入边界存在会直接产生错误结果或 500 响应的高风险缺陷，暂不宜把结果宣称为“严格、准确”的传统排盘结果**。

## 高风险缺陷（P1）

### 1. 月柱算法在大量日期上计算错误

- 位置：`E:\PythonProject\liuyao_paipan\backend\core\ganzhi.py:48-86,123-197`
- 已确认问题：
  1. 五虎遁口诀写明“乙庚之岁戊为头”，但 `month_gan_start_map` 把乙、庚年映射为“庚”，应为“戊”（154-159 行）。
  2. 1 月 1-4 日找不到当前年份之前的最近节气，代码直接判为丑月（140-145 行），实际在小寒前仍属上一子月。
  3. 节气全部固定为约定日期的 00:00，年柱也固定在 2 月 4 日 00:00 切换，无法正确处理真实交节时刻。
- 独立基准：与 `lunar-python 1.4.8` 比较 2020-2029 每日正午共 3,653 个样本，月柱不一致 801 个、年柱不一致 3 个；日柱和正午时柱均一致。
- 影响：五虎遁错误会直接造成月干展示错误；1 月及真实交节边界还会错到月支，继而污染月建生克、月破、暗动及旺衰判断。
- 建议：改用精确历法库（项目依赖中已注释 `sxtwl`）或完整节气算法，修正五虎遁映射，并为交节前后、1 月跨年和子时换日约定建立基准测试。

### 2. “时间起卦”并未实现所声明的梅花易数年月日时规则

- 位置：`E:\PythonProject\liuyao_paipan\backend\api\qigua.py:105-143`；声明见 `E:\PythonProject\liuyao_paipan\README.md:123`
- 代码直接使用公历年份、月份、日期和 0-23 小时求和；传统规则使用年支序数、农历月日及时支序数。
- 原典依据：[《梅花易数·年月日時起例》](https://ctext.org/wiki.pl?chapter=867487&if=gb)。
- 影响：该用户可见功能大多数输入都会得到与所声明规则不同的卦。
- 建议：先把公历时间转换为农历月日、年支和时支，再按原典取上卦、下卦与动爻；加入原典“辰年十二月十七日申时”等固定案例。

### 3. API 没有建立有效的领域输入边界

- 位置：
  - `E:\PythonProject\liuyao_paipan\backend\models\gua.py:72-87`
  - `E:\PythonProject\liuyao_paipan\backend\api\qigua.py:61-101`
  - `E:\PythonProject\liuyao_paipan\backend\api\paipan.py:14-31`
- `yao_list`、`changing_yao`、`timestamp` 只声明为普通列表/字典，没有长度、0/1、日期范围和必需键约束；`manual_complete` 直接接收 `List[dict]`。
- 真实 HTTP 验证：

| 输入 | 实际结果 |
|---|---|
| 5 个爻值 | 500，`list index out of range` |
| 1 个动爻标志 | 500，`list index out of range` |
| 6 个值均为 2 | 500，并回显内部异常 |
| 月 13、日 40、时 99 | 起卦接口先返回 200，排盘再返回 500 |
| 手摇项缺少 `is_changing` | 500 |
| `changing_yao` 长度为 1 | `/specify` 返回 200，并静默改成全静爻 |

- 影响：客户端错误被错误归类为服务端故障，返回不可预测，且内部异常文本直接暴露。
- 建议：使用嵌套 Pydantic 模型、`Literal[0,1]`、固定长度列表和日期时间校验；客户端输入错误统一返回 422/400，服务端日志记录细节但响应不泄露异常。

### 4. 关键算法完全没有自动化回归保障

- 项目内没有测试文件、测试配置、CI、lint 或类型检查配置；CodeGraph 对公共模型、排盘接口和核心算法均报告“no covering tests”。
- 影响：历法映射这类基础错误能长期存在；任何数据表或生克规则改动都无法判断是否破坏 64 卦。
- 建议：最低限度覆盖历法权威样本、64 卦查表、世应/纳甲/六亲、动变、旬空、时间起卦和 API 非法输入，并保留当前 4,096 组合属性测试。

## 中风险缺陷（P2）

### 5. 自动/手摇的动爻概率与代码说明不符

- 位置：`E:\PythonProject\liuyao_paipan\backend\api\qigua.py:15-34`
- `randint(0,5)` 中 2 个结果为动爻，实际概率为 `2/6 = 1/3`，不是注释所写的约 `1/4`。
- 建议：明确采用哪种起卦法；若模拟三枚铜钱，应按四种和值的真实分布实现并做统计测试。

### 6. 省略时间时会固定使用 0 时，而不是当前时刻

- 位置：`E:\PythonProject\liuyao_paipan\backend\models\gua.py:77-80`、`E:\PythonProject\liuyao_paipan\backend\api\qigua.py:111-115`
- `hour` 默认值为 `0`，而接口以“是否为 `None`”决定是否使用当前小时；真实请求 `{"method":"time"}` 返回的时间戳小时为 0。
- 影响：直接调用 API 而非网页时，时间起卦和指定起卦会悄然使用午夜。

### 7. 前端失败处理不完整，会静默失败、串联错误或产生未处理 Promise

- 位置：
  - `E:\PythonProject\liuyao_paipan\frontend\js\qigua.js:38-74,159-180,200-212`
  - `E:\PythonProject\liuyao_paipan\frontend\js\paipan.js:217-255`
- 已确认：
  - 手摇请求没有 `catch` 和状态码判断；浏览器离线验证出现 `Uncaught (in promise)`。
  - 自动/时间起卦不检查 `resp.ok`，错误 JSON 仍会被送入排盘接口。
  - `requestPaipan` 未 `await renderPaipan(...)`，渲染异常逃出当前 `try/catch`。
  - 卦辞请求不检查状态码且空 `catch`；失败时用户无提示，并可能保留上一次结果。
- 建议：统一封装 `fetchJson`、检查状态码、等待渲染完成，增加加载/禁用/错误状态及竞态取消。

### 8. 前端与后端存在两套互相绕开的起卦流程

- 位置：`E:\PythonProject\liuyao_paipan\frontend\js\qigua.js:131-180`
- “手工指定”和“手动摇卦”都直接组装数据调用 `/api/paipan/`，未使用后端已有的 `/api/qigua/specify` 与 `/api/qigua/manual_complete`。
- 影响：校验和默认值逻辑发生漂移，两个后端接口实际上没有网页调用方。
- 建议：只保留一条端到端契约，前端统一调用起卦接口后再排盘，或合并成一个原子排盘接口。

### 9. 备用纳甲模块整体不可用，且与生产数据相互矛盾

- 位置：`E:\PythonProject\liuyao_paipan\backend\core\najia.py:31-189`
- `SHIYING_TABLE` 字面量存在大量重复/错误键，运行后仅剩 42 项；纳地支按相邻地支 `±1` 排列，而数据生成器使用正确的隔位 `±2`。
- 与当前生产 `64gua_full.json` 对比全部 64 卦：22 卦抛异常，42 卦结果不一致，完全一致为 0。
- 当前生产引擎读取 JSON，未调用该模块，因此网页主链路暂未受影响；但任何复用或重构都会立即引入错误。
- 建议：删除该重复实现，或让生产表和运行时都由唯一、已测试的纳甲实现生成。

### 10. 生克模块保留了可调用但必然报错的公共方法

- 位置：`E:\PythonProject\liuyao_paipan\backend\core\shengke.py:100-214,260-289`
- `calc_all_relations()` 仍以两个参数调用已经改成单参数的 `find_sanhe()`；真实调用抛出 `TypeError`。
- 同文件 `_is_wang_single()` 重复定义两次；六合/六冲方法的文档写“两爻均动”，实现和 README 则是“至少一爻动”。
- 建议：删除废弃入口或恢复一致契约，并用领域测试明确冲合成局条件。

### 11. 部分“严格规则”实际只是简化启发式

- 位置：`E:\PythonProject\liuyao_paipan\backend\core\shengke.py:338-417`
- 暗动旺衰只看临月、同五行或月生，不考虑代码注释承认缺失的动爻生扶等全局关系；节气同样采用固定日期近似。
- 这与 `E:\PythonProject\liuyao_paipan\README.md:7` 的“严格遵循传统规则、保证准确性”不一致。
- 建议：把未完整实现的规则明确标为实验性，或由领域专家给出决策表和权威案例后再宣称准确。

### 12. 安全与部署配置缺少最小边界

- 位置：`E:\PythonProject\liuyao_paipan\backend\main.py:25-31`、`E:\PythonProject\liuyao_paipan\backend\api\paipan.py:30-31`
- 同源前端仍配置 `allow_origins=["*"]`、所有方法/请求头并允许凭据；排盘接口把底层异常文本返回客户端。
- 当前应用无账号和持久化，现实影响有限；一旦监听公网或增加敏感功能，风险会放大。
- 建议：本地模式移除 CORS，跨域部署时列出允许来源；对外只返回稳定错误码和通用消息。

## 低风险不足（P3）

1. `E:\PythonProject\liuyao_paipan\backend\core\guaci.py:7-16` 与 `E:\PythonProject\liuyao_paipan\backend\utils\validator.py:4-5` 是空实现；`backend\models\request.py` 又定义了一套未使用请求模型。
2. `E:\PythonProject\liuyao_paipan\backend\models\gua.py:33-52` 重复声明日/月冲合字段，89-99 行还把示例 `relations` 作为模块变量执行。
3. `E:\PythonProject\liuyao_paipan\backend\core\liuyao_engine.py:29-100,313-348` 通过备用类和导入时 monkey patch 补齐模型/计算器，隐藏真实依赖错误并增加导入顺序风险。
4. `buildSpecifyPanel()` 每次切换面板都会再次给同一容器绑定点击监听器（`frontend\js\qigua.js:18,35,78-129`）。
5. 初始页面显示空结果表头；缺少 favicon，浏览器每次加载产生 404；表单标签和当前选中方式也缺少完整可访问性语义。
6. 前端硬编码 64 卦名称到 ID 的第二份映射；当前验证一致，但未来修改后容易漂移。
7. `start.bat` 在虚拟环境不存在时仍继续执行，并始终开启 `--reload`；`backend.main`、两个批处理与 README 的监听地址/热重载行为不一致，名为 `liuyao_start&stop.bat` 的脚本也没有停止逻辑。
8. 项目没有 Git 仓库、CI、依赖哈希锁定或发布配置；`backend\data\generate_64gua_table.py.bak` 等历史文件与重复生成脚本增加维护歧义。
9. `init_project.py:427` 在 Python 3.13 编译时产生无效转义序列警告。

## 已验证通过

| 验证项 | 结果 |
|---|---|
| CodeGraph 初始化 | 30 文件、296 节点、540 边 |
| Python 语法编译 | 后端与两个根脚本通过；仅有 1 个转义警告 |
| JavaScript 语法 | 4 个脚本全部通过 `node --check` |
| 64 卦排盘结构 | 64 个唯一阴阳编码，64 个本卦名、64 个变卦名 |
| 本卦/动爻穷举 | 4,096 组合全部排盘并通过响应模型验证 |
| 辞典数据 | 64 卦、384 爻均存在且无空值/占位符 |
| 前端名称映射 | 与卦辞文王序 ID 全部一致 |
| 真实页面主链路 | 自动起卦 → 排盘 → 本/变卦辞加载成功 |
| 移动端布局 | 390 px 视口无页面级横向溢出 |

## 剩余风险

- 上述验证确认了结构完整性，**没有**完成全部纳甲、世应、伏神、生克冲合、暗动和卦辞文本的权威领域逐项校勘。
- `lunar-python` 是独立实现基准，不替代项目对历法流派、时区、真太阳时和 23 时换日规则的明确决策。
- 未做并发、压力、多浏览器和长期随机分布测试。

## 建议修复顺序

1. 重做历法与时间起卦，并建立权威样本测试。
2. 收紧 Pydantic 契约和错误响应，消除所有非法输入 500。
3. 统一前后端起卦链路并补齐前端失败处理。
4. 删除或修复 `najia.py`、废弃生克入口和空实现。
5. 最后处理 CORS、启动部署、重复代码、UI 与工程化问题。

## 任务账本

- 目标：完成 CodeGraph 初始化与项目代码/功能审查。
- 范围：后端 API、历法与排盘核心、静态数据、前端交互、启动部署和质量保障。
- 已确认事实：详见本报告；项目根目录不是 Git 仓库。
- 决策：未修改业务代码，只新增 CodeGraph 索引与本报告。
- 修改文件：`.codegraph/`、`CODE_REVIEW.md`；运行验证可能刷新 Python `__pycache__`。
- 验证结果：核心结构和主页面链路通过；高、中风险缺陷已用源码、独立基准或运行结果确认。
- 剩余风险：领域规则尚需专家权威样本校勘。
- 下一步：按“建议修复顺序”实施。
